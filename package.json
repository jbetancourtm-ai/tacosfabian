{
  "name": "tacos-fabian-swa",
  "private": true,
  "version": "1.0.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "swa": "swa start http://localhost:5173 --run \"npm run dev\" --api-location api"
  },
  "dependencies": {
    "@azure/data-tables": "^13.3.0"
  },
  "devDependencies": {
    "vite": "^5.4.0"
  }
}
